<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag, tag_path(tag), class: "tag_title", id: "tag_#{tag.id}" %>
    </li>
  <% end %>
</ul>

<p><%= link_to "New Tag", new_tag_path, class: "new_tag" %></p>